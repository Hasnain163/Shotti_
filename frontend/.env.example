# Frontend environment. Anything in a VITE_ variable is public by definition —
# never put an API key here. Backend secrets live in the repo-root .env.

# Leave empty in development: vite.config.ts proxies /api to the backend.
# Set to the deployed backend origin in production, e.g. https://api.example.com
VITE_API_BASE_URL=
